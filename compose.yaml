services:
  service-monitor:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    environment:
      - LISTEN_ADDR=:3001
      - DB_PATH=/app/data/service-monitor.db
      - SECRET_KEY=change-me-in-production
    restart: unless-stopped
